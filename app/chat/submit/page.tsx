"use client";

import React, { Suspense } from "react";
import FormSubmit from "@/app/components/chat/submit";

const Submit = () => {
  return(
    <Suspense>
      <FormSubmit/>
    </Suspense>
  )
};

export default Submit;
